/**
 * @file views
 * @brief Forwarding header for <code>jh::views</code>.
 *
 * <p>
 * This header provides the canonical include entry for
 * JH range view adaptors.
 * </p>
 *
 * <pre><code>#include &lt;jh/views&gt;</code></pre>
 *
 * <p>
 * In the current <strong>1.3.x</strong> series, this file simply forwards to
 * <code>jh/views.h</code>, which performs the actual aggregation of
 * view adaptors (e.g. <code>zip</code>, <code>enumerate</code>) defined in
 * <code>jh/ranges/views/&#42;.h</code>.
 * </p>
 *
 * <h4>Evolution Note (1.4.0+)</h4>
 * <ul>
 *   <li>Starting with <strong>version&nbsp;1.4.0</strong>,
 *       <code>jh/views</code> itself will become a <strong>forward-aggregator</strong>
 *       header.</li>
 *   <li>The previous <code>jh/views.h</code> file will be removed.</li>
 *   <li>All adaptor implementations will continue to reside under
 *       <code>jh/ranges/views/&#42;.h</code>; only the include entry changes.</li>
 *   <li><code>#include&nbsp;&lt;jh/views&gt;</code> is the
 *       <strong>permanent and preferred</strong> include form.</li>
 * </ul>
 *
 * <p>
 * This header remains stable and is not deprecated.
 * </p>
 */

#pragma once

#include "jh/views.h"
