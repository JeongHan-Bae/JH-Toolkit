/**
 * @file data_sink
 * @brief Forwarding header for jh::data_sink
 *
 * This header exists to provide a modern include style (`#include <jh/data_sink>`),
 * forwarding to `jh/data_sink.h`. It contains no additional code.
 */

#pragma once

#include "jh/data_sink.h"
