/**
 * @file pool
 * @brief Forward-aggregator header for all <code>jh::&#42;_pool</code>s.
 *
 * <p>
 * This header provides the canonical include entry for
 * <code>jh::&#42;_pool&lt;...&gt;</code>s &mdash; a high-level, user facing
 * around <code>jh::conc::&#42;_pool&lt;...&gt;</code> pool bases.
 * </p>
 *
 * <pre><code>#include &lt;jh/pool&gt;</code></pre>
 *
 * <p>
 * Internally, it includes the user facing pool interfaces from
 * <code>jh/concurrent/&#42;.h</code> and exposes them collectively.
 * </p>
 */

#pragma once

#include "jh/concurrent/observe_pool.h"
#include "jh/concurrent/resource_pool.h"
