/**
 * @file runtime_arr
 * @brief Forwarding header for <code>jh::runtime_arr</code>.
 *
 * <p>
 * This header provides the canonical include entry for
 * <code>jh::runtime_arr&lt;T, Alloc&gt;</code> &mdash; a runtime-sized,
 * fixed-capacity array container.
 * </p>
 *
 * <pre><code>#include &lt;jh/runtime_arr&gt;</code></pre>
 *
 * <p>
 * Internally, this file forwards to <code>jh/core/runtime_arr.h</code>
 * and defines no additional symbols.
 * </p>
 */

#pragma once

#include "jh/core/runtime_arr.h"
