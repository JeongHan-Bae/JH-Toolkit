/**
 * @file pod_stack
 * @brief Forwarding header for jh::pod_stack.
 *
 * This header exists to provide a modern include style (`#include <jh/pod_stack>`),
 * forwarding to `jh/pod_stack.h`. It contains no additional code.
 */

#pragma once

#include "jh/pod_stack.h"
